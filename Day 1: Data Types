
  var x = "";
  var y = "";
  var z = "";
// Declare second integer, double, and String variables.

    x = parseInt(readLine()) + i;

y = parseFloat(readLine()) + d;

z = s + readLine();// Read and save an integer, double, and String to your variables.

  console.log(x);  // Print the sum of both integer variables on a new line.

   console.log(y.toFixed(1)); // Print the sum of the double variables on a new line.

  console.log(z);  // Concatenate and print the String variables on a new line
    // The 's' variable above should be printed first.
